{
  "name": "playwright-poc",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/main.js",
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "npm run clean && tsc && copyfiles -f src/index.html dist",
    "start": "npm run compile && cross-env NODE_ENV=development electron .",
    "test-e2e": "playwright test",
    "test-e2e-open": "playwright test --headed",
    "test-e2e-debug": "playwright test --debug",
    "test-e2e-codegen": "playwright codegen"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.31.2",
    "copyfiles": "^2.4.1",
    "cross-env": "^7.0.3",
    "electron": "^23.1.4",
    "typescript": "^5.0.2"
  }
}
